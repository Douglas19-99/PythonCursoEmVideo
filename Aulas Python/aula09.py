# frase = 'Curso em Video Python'

# print(frase[9:13]) = Começa na letra 9 e para na 13 (pega somente até a 12)
# print(frase[9:21:2]) = Pula de duas em duas letras.
# print(frase[:5]) = Começa no ZERO e para no 5.
# print(frase[15:]) = Começa na 15 e vai até o final.
# print(frase[9::3]) = Começa no 9 e vai até o final pulando de 3 em 3.

# Análise:

# len(frase) = Contas quantos caracteres tem na frase.

# print(frase.count('o', 0, 13)) = quantas vezes a letra O aparece na frase.
# Conta quantas vezes 'o' tem entre 0 e 13.

# frase.finde('deo') = Indica em qual posição estão esses caracteres.
# (caso não tenha o valor na frase, vai me retornar o valor de -1.)

# print = (frase('curso' ind frase)) = Vai verificar se 'curso' está na frase e me
# retorna True ou False.

# Transformação:

# frase.replace('Python', 'Android') = Substitui python por android na frase.

# frase.upper() = Muda toda a frase em maiúscula.

# frase.lower() = Muda toda a frase em minúscula.

# frase.capitalize() = Deixa apenas a primeira letra da frase maiúscula.

# frase.title() = Muda a primeira letra de cada palavra para maiúsucula.

# frase.strip() = Elimina espaços inúteis no começo e no fim. (Colocar L em strip,
# elimina espaços somente a esquerda. Colocar R elimina espaços somente a direita.)

# Divisão:

# frase.split() = Divide uma str em partes usando, por padrão, os espaços em branco
# como separadores. O resultado todo é uma lista com as partes separadas.

# Junção:

# '-'.join(frase) = Método usado para juntar os elementos de uma lista str em uma
# única str, com um separador definido.
